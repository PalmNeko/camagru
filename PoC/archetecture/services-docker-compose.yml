
networks:
  external-network: &network
    driver: bridge
    attachable: false
    internal: false
  frontend-network: &internal
    <<: *network
    internal: false
  backend-network: *internal

volumes:
  uploads_volume:
