

services:
  mariadb:
    image: mariadb
    container_name: ${MARIADB_CONTAINER_NAME:-mariadb}
    hostname: ${MARIADB_HOSTNAME:-mariadb}
    restart: always
    environment:
      MARIADB_DATABASE: ${MARIADB_DATABASE:-camagru}
      MARIADB_USER: ${MARIADB_USER:-camagru}
    volumes:
      - ${SERVICES_MARIADB_LOCAL_DIR:-.}/docker-entrypoint-initdb.d/:/docker-entrypoint-initdb.d/
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
      start_period: 10s
      interval: 10s
      timeout: 5s
      retries: 3
